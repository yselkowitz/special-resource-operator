apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fpga-xrt-driver.fullname" . }}-test-connection"
  labels:
    {{- include "fpga-xrt-driver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fpga-xrt-driver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
